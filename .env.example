# Backend that serves POST /api/waitlist (used by src/components/sections/Final.jsx).
# The original Emergent deployment had a FastAPI backend behind this URL; it is NOT
# part of this copy. Point it at your own API, or the waitlist form will fail.
REACT_APP_BACKEND_URL=http://localhost:8000
